package com.example.paintp;

/*
import javafx.scene.control.Label;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;
import static org.junit.jupiter.api.Assertions.*;


public class HelloControllerTest {

    private HelloController controller;

    @BeforeEach
    public void setUp() {
        controller = new HelloController();
    }

    @Test
    public void testPenSize() {
        controller.setPenSize(5.0);
        assertEquals(5.0, controller.getPenSize());
    }

    @Test
    public void testUpdateLabel() {
        controller.infoText = new Label();
        controller.updateLabel(100, 150);
        assertTrue(controller.infoText.getText().contains("X: 100, Y: 150"));
    }

    @Test
    public void testAutosaveTimeChange() {
        controller.autosaveInterval = 60;
        controller.onChangeAutosaveTimeClick();  // Simulating the user input dialog
        assertEquals(60, controller.autosaveInterval);
    }
}



 */

